# PetReady PRD

| 文档信息 | 内容 |
| --- | --- |
| 产品名称 | PetReady |
| 产品形态 | 移动端优先 Web 应用 |
| 当前版本 | V1.0（基于现有项目实现整理） |
| 文档状态 | 可评审 |
| 更新时间 | 2026-09-13 |

## 1. 产品概述

PetReady 是一款面向潜在养宠人的决策支持工具。用户通过回答 18 个与时间、居住、预算、支持系统和照护偏好有关的问题，获得以下结果：

1. 当前是否具备开始养宠的基础条件；
2. 当前生活方式更适合猫、狗、猫狗均可，还是暂不建议养宠；
3. 最多 3 个相对适合的品种候选及匹配理由；
4. 与当前条件冲突较大的品种及冲突原因；
5. 下一步行动计划和候选品种的时间、费用投入范围。

产品不替用户作出最终决定，也不以品种标签替代对具体个体的线下接触、健康检查与行为评估。

## 2. 背景与问题

潜在养宠人在决策时容易受到外貌、社交媒体热度或单一品种印象影响，忽略养宠是一项长期、持续且具有现实约束的责任。常见问题包括：

- 未确认住所政策或家庭成员意见便开始养宠；
- 低估日常陪伴、运动、训练和清洁所需时间；
- 只考虑基础月费，忽略突发医疗和离家照护安排；
- 把品种平均倾向当成个体性格和健康状况的保证；
- 推荐工具只给出“适合什么”，缺少“不适合什么”及对应原因。

本产品通过结构化问卷与透明的规则判断，帮助用户先判断“现在适不适合养”，再判断“更适合养什么”。

## 3. 产品目标

### 3.1 核心目标

- 在 3～5 分钟内完成一次无需登录的养宠条件评估；
- 优先识别不适合立即养宠的硬性阻断条件，降低冲动决策；
- 给出可解释的猫狗方向和品种匹配结论；
- 同时呈现匹配点、冲突点、现实成本与下一步行动；
- 仅在用户本机保存答题进度，不建立用户画像或身份档案。

### 3.2 非目标

- 不提供医疗诊断、疾病预测或兽医建议；
- 不保证特定品种或个体一定适合用户；
- 不替代领养机构、正规繁育机构、训犬师或执业兽医的专业评估；
- 不提供宠物交易、领养撮合、商品购买或保险销售；
- V1.0 不支持账号、跨设备同步、社交分享和后台运营系统。

## 4. 目标用户

### 4.1 核心用户

- 第一次考虑养猫或养狗、缺乏系统认知的人；
- 已有偏好品种，但希望验证其是否适配当前生活的人；
- 正在猫狗之间犹豫，希望获得生活方式导向建议的人；
- 家庭成员共同讨论养宠，需要一份客观检查清单的人。

### 4.2 典型场景

**场景 A：冲动养宠前冷静判断**  
用户因喜欢某个品种而准备购买或领养，希望快速确认住所、预算和时间条件是否满足。

**场景 B：猫狗方向选择**  
用户没有明确物种偏好，希望根据独处时长、运动时间、互动投入和噪声接受度获得方向建议。

**场景 C：候选品种取舍**  
用户在多个候选之间比较，希望看到活动量、掉毛、叫声、护理、费用等差异。

**场景 D：暂缓后的改善计划**  
用户暂不具备养宠条件，希望知道应先解决哪些问题，以及何时适合重新评估。

## 5. 产品原则

1. **先准备度，后品种。** 硬性前置条件不满足时，不用低匹配品种凑推荐结果。
2. **解释优先。** 每个结论必须呈现可理解的生活依据或冲突原因。
3. **现实优先。** 时间、住所、预算与可靠照护优先于外貌和流行度。
4. **承认个体差异。** 品种数据只表示常见倾向，不构成对个体的保证。
5. **最少数据。** 无需登录，不收集身份信息，不长期保存用户答案到服务端。
6. **谨慎表达。** 避免使用“绝对适合”“低敏”“保证健康”等误导性结论。

## 6. 核心用户流程

```text
首页
  ↓
18 题生活评估（支持本机续答）
  ↓
完整性校验
  ↓
准备度判断 ── 不满足硬条件 ──→ 暂缓结论 + 阻断原因 + 改善行动
  ↓ 满足基本条件
猫狗方向判断
  ↓
20 个品种逐一匹配与排序
  ↓
结果页：推荐候选 + 冲突候选 + 行动计划 + 费用估算
  ↓
品种详情 / 最多 3 个候选对比 / 返回修改答案
```

## 7. 功能需求

### 7.1 首页

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| HOME-01 | 展示产品价值、评估耗时、无需登录和保存方式 | P0 | 用户进入首页即可理解产品用途及完成成本 |
| HOME-02 | 提供“进入我的生活评估”入口 | P0 | 点击后进入第 1 题 |
| HOME-03 | 检测未完成的本机答题记录 | P0 | 存在 1～17 道已答题目时显示“继续上次评估”和完成进度 |
| HOME-04 | 新一轮评估初始化 | P0 | 无未完成记录时点击开始，答案和当前题号从头初始化 |

### 7.2 问卷

问卷共 18 道必答单选题，分为 4 个章节：

| 章节 | 题数 | 评估维度 |
| --- | ---: | --- |
| 01 时间与陪伴 | 5 | 养宠时间线、独处时长、户外运动、互动训练、出行频率 |
| 02 居住与环境 | 6 | 家庭共识、住所许可、空间、儿童/老人、过敏、噪声接受度 |
| 03 预算与支持 | 3 | 日常预算、医疗备用金、离家照护方案 |
| 04 照护与偏好 | 4 | 猫狗偏好、掉毛、护理投入、相处期待 |

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| QUIZ-01 | 逐题展示题目和单选项 | P0 | 每次仅展示 1 道题，选中项有明确状态 |
| QUIZ-02 | 显示章节、总进度和章内进度 | P0 | 进度随当前题号正确变化，范围为 1/18～18/18 |
| QUIZ-03 | 必答校验 | P0 | 未选择答案时“下一题”不可用，服务端也校验 18 题完整性 |
| QUIZ-04 | 支持上一题和修改答案 | P0 | 返回后显示原选项，修改后覆盖旧答案 |
| QUIZ-05 | 自动保存进度 | P0 | 每次答题或切题后将答案、题号和更新时间保存到 localStorage |
| QUIZ-06 | 完成后提交评估 | P0 | 第 18 题提交成功后进入结果页；失败时展示可恢复的错误提示 |

### 7.3 准备度判断

准备度分为三级：

| 等级 | 展示文案 | 判定规则 |
| --- | --- | --- |
| delay | 建议暂缓养宠 | 命中任一硬性阻断条件 |
| conditional | 有条件适合 | 无硬性阻断，但软性风险不少于 2 项 |
| ready | 适合开始准备 | 无硬性阻断，且软性风险少于 2 项 |

硬性阻断条件：

- 同住成员中有人明确反对；
- 当前住所明确禁止养宠；
- 目前无法稳定承担日常支出；
- 经常出差或旅行，且没有可靠照护方案。

软性风险包括：

- 家庭成员尚未充分讨论；
- 住所允许范围未知或存在限制；
- 尚无离家照护方案；
- 目前无法准备突发医疗备用金。

要求：命中硬性阻断时，猫狗方向必须为“当前猫狗均不建议”，推荐品种数量必须为 0，并输出最多 7 条可执行改善行动。

### 7.4 猫狗方向判断

方向结果包括：更适合猫、更适合狗、猫狗均可、当前猫狗均不建议。

系统根据以下因素对猫狗方向进行加权：

- 每日可提供的户外运动时间；
- 工作日连续独处时长；
- 每日互动和训练投入；
- 对叫声与邻里影响的接受度；
- 用户当前猫狗偏好。

当猫狗得分差的绝对值不超过 1 时，结果为“猫狗均可”。方向结果必须同时展示最多 4 条形成判断的原因。

### 7.5 品种匹配

V1.0 品种库共 20 个品种，其中猫 8 个、狗 12 个。每个品种包含：

- 中英文名、常用别名、物种和体型；
- 活动量、独处耐受、叫声、掉毛、护理、训练、家庭适配倾向；
- 日常月费范围和每日投入时长；
- 适合与不适合的生活情境；
- 图片和健康风险提示。

每个品种以 60 分为初始匹配分，根据生活条件加减分，并限制在 0～100 分。判断需覆盖：

- 住所限制与犬只体型；
- 居住空间与大型犬；
- 可提供运动时间与品种活动量；
- 连续独处时长与独处耐受；
- 掉毛接受度与品种掉毛倾向；
- 安静需求与品种叫声倾向；
- 护理投入与美容护理需求；
- 稳定月预算与品种费用下限；
- 品种物种与猫狗方向的一致性。

匹配状态：

| 状态 | 规则 | 展示等级 |
| --- | --- | --- |
| excluded | 命中硬性品种冲突 | 不建议 |
| caution | 存在至少 2 项冲突，或分数低于 55 | 需要谨慎 |
| eligible | 未命中以上条件 | 75 分及以上为高匹配，否则为中等匹配 |

推荐候选必须同时满足：状态为 `eligible`、至少有 2 条匹配理由，按得分降序展示，最多 3 个。若没有满足条件的品种，明确告知用户“暂未找到合适品种”，不得用低匹配结果补足数量。

### 7.6 结果页

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| RESULT-01 | 展示准备度和猫狗方向 | P0 | 结论首屏可见，并明确为生活条件判断 |
| RESULT-02 | 展示现实依据 | P0 | 合并准备度与方向原因，去重后最多展示 4 条 |
| RESULT-03 | 展示推荐候选 | P0 | 非暂缓状态展示 0～3 个合格候选；每个候选含匹配等级、理由、取舍、时间与费用 |
| RESULT-04 | 展示冲突候选 | P0 | 展示 2～3 个不建议或需谨慎候选及主要冲突原因 |
| RESULT-05 | 展示行动计划 | P0 | 根据家庭共识、住所、医疗金、照护安排等输出去重行动项 |
| RESULT-06 | 展示费用估算 | P1 | 有推荐候选时显示候选集合的稳定期月费和首年估算区间，并注明估算边界 |
| RESULT-07 | 支持修改答案 | P0 | 返回问卷后保留答案，重新提交可刷新结果 |
| RESULT-08 | 保存最近结果 | P1 | 评估结果保存于 localStorage，刷新或返回结果页后仍可查看 |

首年费用估算规则：

- 稳定期月费范围取推荐候选中的最低下限和最高上限；
- 首年下限 = 稳定期最低月费 × 12 + 基础一次性成本；
- 首年上限 = 稳定期最高月费 × 12 + 较高一次性成本；
- 候选含犬时，下限一次性成本按 ¥1,200 计，否则按 ¥800 计；
- 候选含大型宠物时，上限一次性成本按 ¥5,000 计，否则按 ¥3,500 计；
- 不包含宠物购买价格，且必须提示城市和个体差异会改变区间。

### 7.7 品种详情

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| BREED-01 | 展示品种基础信息 | P0 | 包含图片、中英文名、别名、物种、体型 |
| BREED-02 | 展示用户个性化匹配信息 | P0 | 已完成评估时显示匹配等级、匹配理由、冲突与取舍 |
| BREED-03 | 展示关键特征 | P0 | 展示活动量、独处耐受、叫声、掉毛、护理和训练 6 个维度 |
| BREED-04 | 展示适配场景和投入 | P0 | 展示适合/不适合的生活、每日投入和稳定期月费 |
| BREED-05 | 展示健康边界提示 | P0 | 明确本产品不提供具体疾病风险判断，并建议咨询执业兽医 |

### 7.8 品种对比

| 编号 | 需求 | 优先级 | 验收标准 |
| --- | --- | --- | --- |
| COMPARE-01 | 从推荐候选加入或移出对比 | P1 | 仅允许结果页推荐候选进入对比 |
| COMPARE-02 | 限制对比数量 | P1 | 最少 2 个才能形成表格，最多同时选择 3 个 |
| COMPARE-03 | 逐维度横向比较 | P1 | 比较体型、每日投入、活动量、独处耐受、掉毛、叫声、护理和每月费用 |
| COMPARE-04 | 避免绝对排名 | P1 | 页面说明对比只呈现差异，不作绝对优劣判断 |

## 8. 信息架构与路由

| 路由 | 页面 | 前置条件 |
| --- | --- | --- |
| `/` | 首页 | 无 |
| `/quiz` | 生活评估问卷 | 已加载题库 |
| `/result` | 评估结果 | 存在有效评估结果，否则跳转问卷 |
| `/breeds/:id` | 品种详情 | 品种 ID 有效 |
| `/compare` | 品种对比 | 无；不足 2 个候选时展示空状态 |

## 9. 接口需求

### 9.1 获取题库

`GET /api/questions`

返回题目列表和章节列表。题目包含 `id`、标题、章节、可选帮助文案、选项及必答标识。

### 9.2 获取品种库

`GET /api/breeds`

返回完整品种数据，用于结果卡片、详情和对比页展示。

### 9.3 提交评估

`POST /api/assess`

请求体：

```json
{
  "answers": {
    "timeline": "soon",
    "aloneHours": "medium"
  }
}
```

约束：请求必须包含全部 18 道题的有效答案。缺少答案时返回 HTTP 400、错误文案及缺失题目 ID；无效 JSON 返回 HTTP 400。

成功响应包含：

- `readiness`：准备度等级、标题、原因和行动；
- `direction`：猫狗方向、标题和原因；
- `matches`：全部 20 个品种的匹配结果；
- `recommended`：最多 3 个推荐候选；
- `negative`：冲突候选；
- `actions`：最多 7 条去重行动建议。

## 10. 数据与隐私

- 不要求登录，不采集姓名、手机号、邮箱或精确位置；
- 问卷答案与最近一次结果保存在浏览器 localStorage；
- 服务端仅接收完成本次规则计算所需的答案，不应持久化请求体、创建用户档案或用于广告画像；
- 日志不得记录完整答案组合；如需监控，只记录匿名的接口状态、延迟和错误类型；
- 提供清除本机评估记录的能力列为 V1.1 P0；
- 当前页面“本地计算”文案与实际由服务端执行评估不完全一致，发布前应统一改为“本机保存 · 不收集身份信息”，或将评估逻辑真正迁移至浏览器端。

## 11. 非功能需求

### 11.1 可用性

- 首屏核心行动按钮在常见手机尺寸下无需横向滚动；
- 所有交互按钮具备清晰的默认、选中、禁用和点击反馈；
- 网络失败时保留已答内容，并提供明确的重试路径；
- 页面刷新后可恢复未完成问卷和最近结果。

### 11.2 可访问性

- 使用语义化标题层级与按钮元素；
- 图片提供有效替代文本，纯装饰图片使用空替代文本；
- 键盘可完成答题、返回、查看详情和对比；
- 选中状态不得只依赖颜色表达；
- 正文、按钮和状态标签满足 WCAG 2.1 AA 的颜色对比度目标。

### 11.3 性能与兼容性

- 支持 Node.js 18 及以上版本；
- 目标环境：近两个主要版本的 Chrome、Safari、Edge 和主流移动浏览器；
- 常规网络条件下接口 P95 响应时间目标小于 500 ms；
- 静态资源应使用合理压缩和缓存策略。API 与用户结果不得缓存；
- 单次评估请求体上限为 200 KB。

### 11.4 安全性

- 服务端对题目完整性和选项合法性进行双重校验；
- 静态文件访问必须阻止目录穿越；
- 动态展示文本进行 HTML 转义；
- 不在客户端或服务端日志中输出用户完整答案；
- 增加基础安全响应头列为 V1.1 P0。

## 12. 成功指标

V1.0 上线后建议以匿名、非身份化事件统计以下指标；在埋点上线前须先更新隐私说明：

| 指标 | 定义 | 建议目标 |
| --- | --- | ---: |
| 问卷开始率 | 点击开始评估人数 / 首页访问人数 | ≥ 35% |
| 问卷完成率 | 成功生成结果人数 / 开始评估人数 | ≥ 65% |
| 中途恢复完成率 | 从本机记录继续且最终完成的人数 / 继续评估人数 | ≥ 50% |
| 结果深度浏览率 | 进入品种详情或对比的人数 / 结果页人数 | ≥ 30% |
| 行动建议触达率 | 浏览到行动计划区域的人数 / 结果页人数 | ≥ 60% |
| 接口成功率 | `/api/assess` 成功请求 / 有效提交请求 | ≥ 99.5% |

注意：产品的价值不是提高某一品种的推荐率，而是帮助用户形成更现实、可解释的决策，包括接受“暂缓养宠”的结论。

## 13. 边界与风险

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 规则过度简化真实养宠条件 | 用户把结果当作确定答案 | 持续展示免责声明、判断依据和个体差异提示 |
| 品种费用和倾向随城市、来源、年龄变化 | 估算偏差 | 标明范围与口径，定期复核数据，不给单点承诺 |
| 品种标签强化刻板印象 | 忽略个体性格和经历 | 强调常见倾向，建议线下接触并评估具体个体 |
| 医疗风险信息不足 | 用户低估医疗成本 | 不做疾病诊断；引导咨询执业兽医并建立备用金 |
| 服务端仅检查“是否有值”，未检查选项是否合法 | 恶意或异常输入影响结果 | V1.1 增加基于题库枚举的合法值校验 |
| 前端宣称“本地计算”与当前架构不符 | 隐私表达不准确 | 上线前修正文案或迁移计算逻辑 |
| localStorage 被清理或处于隐私模式 | 答题记录丢失 | 明确“仅保存在本机”，失败时不阻断正常答题 |

## 14. 上线验收标准

### 14.1 核心链路

- 用户可从首页完成全部 18 道题并成功进入结果页；
- 问卷中途刷新或离开后，可在同一浏览器恢复进度；
- 缺少任一必答题时，前端不能正常提交，服务端返回 400；
- 命中任一准备度硬性阻断条件时，结果为“建议暂缓养宠”、方向为“当前猫狗均不建议”、推荐列表为空；
- 正常条件下推荐不超过 3 个，且不包含 `excluded` 品种；
- 用户可查看推荐或冲突候选详情；
- 用户可选择 2～3 个推荐候选进行横向比较；
- 修改答案并重新提交后，所有结论基于新答案更新。

### 14.2 质量门槛

- 自动化规则测试全部通过；
- 关键手机和桌面尺寸无布局溢出或按钮遮挡；
- 所有品种图片加载失败时仍能显示可理解的降级状态；
- API 异常时不丢失已填写答案；
- 页面隐私文案与真实数据处理方式一致；
- 不出现医疗诊断、绝对适配或保证性文案。

## 15. 版本规划

### V1.0：完成当前核心闭环

- 18 题生活评估；
- 准备度、猫狗方向和品种匹配；
- 推荐、冲突、行动计划和费用估算；
- 品种详情与最多 3 个候选对比；
- 本机进度和最近结果保存。

### V1.1：可信度与可用性增强

- 增加“清除本机记录”入口；
- 校验答案值必须来自对应题目的合法选项；
- 增加结果页规则说明和费用口径说明；
- 补充无障碍、错误重试、安全响应头和关键路径测试；
- 建立品种数据来源、复核时间和版本记录；
- 统一隐私与计算位置文案。

### V1.2：验证与个性化增强

- 提供“为什么是这个结论”的可展开分项解释；
- 支持将行动计划导出为本地清单；
- 增加领养成年个体、幼宠和多宠家庭的差异化问题；
- 在获得明确同意且完成隐私评审后，引入匿名化产品分析。

## 16. 待产品评审问题

1. V1.0 上线范围是仅面向中国大陆用户，还是需要覆盖其他地区的费用和住所语境？
2. 品种倾向、费用区间和每日投入由谁审核，建议多久复核一次？
3. 是否将“过敏顾虑”和“儿童/老人”纳入更明确的风险结论，而不仅作为信息采集项？
4. 是否将规则计算迁移到浏览器端，以完全匹配“本地计算”的产品承诺？
5. 是否需要为“暂缓养宠”用户提供可勾选的准备清单和重新评估提醒？
